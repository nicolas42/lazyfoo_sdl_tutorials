{
	// list of all markup files to load.
	// order can matter - later files may override keys from
	// earlier ones (but that is not intentionally used here).

	importJSON:
	[
		Colors,
		Meters,
		Templates,

	 	MainWindow,
		DeviceList,
		DeviceEditor,
		PatchboardView,

		SourceView,
		OutputView,
		MonitorView,
		WireView,
	]
}
